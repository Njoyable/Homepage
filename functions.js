function timeAdd(start1,start2,end1,end2) {
    var hoursStart = start1*60;
    var hoursEnd = end1*60;
    var finalH
    var finalMin

    while(hoursStart>59) {
        finalH++;
        hoursStart -= 60;
    }
}
